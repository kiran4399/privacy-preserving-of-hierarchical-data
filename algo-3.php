

init_mat(sizeof($U))

for($i = 0; $i<5; $i++){
	for($j = 1; $j<sizeof(U); $j++){
		f' = clone(S[i])
		v' = clone(U[j])
		c_ij = diversify(f', v')

	}
}


for($i = sizeof(S)+1; $i<sizeof(U); $i++){
	for($j = 1; $j<sizeof(U); $j++){
		f' = clone(S[i]);
		v' = clone(U[j]);
		c_ij = diversify(f', v')
		
	}
}

X = lsap_solver(C)
$p = 0

foreach($x, $element){
	if ($element ==1 and i <= sizeof(S)){
		P = union(P, S, U);
	}
}